# Large media files — don't commit these, they're generated/downloaded, not source
source-video/*
working-video/*
output-gif/*

# But keep the folders themselves tracked in git (empty dirs aren't tracked by default)
!source-video/.gitkeep
!working-video/.gitkeep
!output-gif/.gitkeep

# Common junk
.DS_Store
*.log